# ============================================================================
# Required Configuration
# ============================================================================

# Discord bot token
DISCORD_TOKEN=your_discord_bot_token_here

# HTTP endpoint URL to forward events to
HTTP_ENDPOINT=https://your-webhook-endpoint.example.com/webhook

# ============================================================================
# Optional Configuration
# ============================================================================

# Enable insecure mode (skip TLS certificate validation)
# WARNING: Only use this for testing with self-signed certificates
# INSECURE_MODE=false

# HTTP client timeout configuration (in seconds)
# HTTP_TIMEOUT=300                # Request timeout (default: 300s / 5 minutes)
# HTTP_CONNECT_TIMEOUT=10         # Connection timeout (default: 10s)

# Logging level
# RUST_LOG=gatehook=info,serenity=warn

# ============================================================================
# Event Handlers
# ============================================================================
# Format: <EVENT_NAME>_<CONTEXT>=<allowed_subjects>
#
# CONTEXT:
#   DIRECT - Direct messages (1-on-1 and group DMs)
#   GUILD  - Guild (server) messages
#   (none) - Context-independent events (e.g., READY)
#
# Allowed subjects (comma-separated):
#   self    - Bot's own messages
#   webhook - Messages from webhooks
#   system  - Discord system messages
#   bot     - Messages from other bots
#   user    - Messages from human users
#   all     - All of the above
#   (empty) - Everything except self (default: user,bot,webhook,system)
#
# If an environment variable is not set, that handler will not be registered.
#
# Examples:
#   MESSAGE_DIRECT=user              # Direct messages: Only humans
#   MESSAGE_GUILD=user,bot           # Guild messages: Humans and other bots
#   MESSAGE_DIRECT=all               # Direct messages: Everything including self
#   MESSAGE_GUILD=                   # Guild messages: Everything except self
# ============================================================================

# ----------------------------------------------------------------------------
# Direct Message Events
# ----------------------------------------------------------------------------
MESSAGE_DIRECT=user,bot,webhook,system

# ----------------------------------------------------------------------------
# Guild (Server) Events
# ----------------------------------------------------------------------------
MESSAGE_GUILD=user

# ----------------------------------------------------------------------------
# Message Delete Events (no filtering available)
# ----------------------------------------------------------------------------
# NOTE: Delete events only provide message IDs (no content, author, etc.)
#
# MESSAGE_DELETE_DIRECT=all     # Single message deletion in DMs
# MESSAGE_DELETE_GUILD=all      # Single message deletion in guilds
# MESSAGE_DELETE_BULK_GUILD=all # Bulk message deletion in guilds (guild-only)

# ----------------------------------------------------------------------------
# Message Update Events (no filtering available)
# ----------------------------------------------------------------------------
# NOTE: Update events only provide changed fields (partial data)
#
# MESSAGE_UPDATE_DIRECT=all     # Message edit/update in DMs
# MESSAGE_UPDATE_GUILD=all      # Message edit/update in guilds

# ----------------------------------------------------------------------------
# Reaction Add Events
# ----------------------------------------------------------------------------
# NOTE: Reaction events support sender filtering (user, bot, self)
# NOTE: Webhook and system types are not applicable to reactions
#
# REACTION_ADD_DIRECT=user      # Reaction added in DMs
# REACTION_ADD_GUILD=user       # Reaction added in guilds

# ----------------------------------------------------------------------------
# Reaction Remove Events
# ----------------------------------------------------------------------------
# NOTE: Reaction events support sender filtering (user, bot, self)
# NOTE: Webhook and system types are not applicable to reactions
#
# REACTION_REMOVE_DIRECT=user   # Reaction removed in DMs
# REACTION_REMOVE_GUILD=user    # Reaction removed in guilds

# ----------------------------------------------------------------------------
# Context-Independent Events
# ----------------------------------------------------------------------------
# READY=all
